#pragma once

#include <QMainWindow>

namespace SilverBell::Application
{

    // 程序主窗口
    class FApplication : public QMainWindow
    {
    public:
        FApplication();
        ~FApplication() override;

    };

}
